$(document).ready(function() {
  /*$.getJSON('/', function(result) {
    var newDiv = `
    <div class="article">
      <h3>${result.title}</h3>
      <small>${result.byline}</small>
      <p>${result.summary}<p>
      <a href="${result.link}">Link</a>
    </div>
    `;
    $('#content').append(newDiv);
  })
  */


});